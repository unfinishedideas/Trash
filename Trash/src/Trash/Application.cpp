#include "Application.h"

namespace Trash {
	Application::Application() {};
	Application::~Application() {};

	void Application::Run() {
		while (true);		// This is just temporary, don't worry ;)
	};
}
